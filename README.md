# Marginalia

A tiny Notion widget: one motivational / philosophical quote per day, with a shuffle for another.
Single self-contained `index.html`, no build, no dependencies.

Embed the GitHub Pages URL in Notion with the `/embed` block.
